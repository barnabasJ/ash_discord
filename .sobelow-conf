[
  verbose: false,
  private: true,
  skip: false,
  exit: "medium",  # Only exit on medium+ severity issues
  format: "txt",
  threshold: "low",  # Still report low confidence issues but don't fail
  ignore: ["Config.HTTPS"],  # Ignore existing issues for now
  ignore_files: ["test/"]
]
